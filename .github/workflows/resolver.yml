name: Resolver CI

on:
  push:
    branches: [ main, master, develop ]
    paths:
      - 'resolver-go/**'
      - 'go.work'
      - 'go.work.sum'
      - '.github/workflows/resolver.yml'
  pull_request:
    branches: [ main, master, develop ]
    paths:
      - 'resolver-go/**'
      - 'go.work'
      - 'go.work.sum'
      - '.github/workflows/resolver.yml'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.22.x'
        cache-dependency-path: |
          resolver-go/go.sum
          go.work.sum

    - name: Cache Go modules
      uses: actions/cache@v3
      with:
        path: |
          ~/.cache/go-build
          ~/go/pkg/mod
        key: ${{ runner.os }}-go-resolver-${{ hashFiles('resolver-go/go.sum', 'go.work.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-resolver-
          ${{ runner.os }}-go-

    - name: Download dependencies
      run: go mod download
      working-directory: resolver-go

    - name: Build
      run: go build ./resolver-go/...

    - name: Test
      run: go test -v ./resolver-go/...

    - name: Race detector
      run: go test -race ./resolver-go/...

    - name: Coverage
      run: go test -coverprofile=coverage.out ./resolver-go/...

    - name: Upload coverage
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.out
        flags: resolver